import { atom } from "recoil";

export const clickedIndexState = atom({
  key: "clickedIndex",
  default: -1,
});

export const clickedPopularIndexState = atom({
  key: "clickedIndex",
  default: -1,
});
